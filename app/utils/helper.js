define(
[
'underscore',
'backbone'
],

function(
_,
Backbone
) {

var helper={
	// eventbus:_.extend({}, Backbone.Events),
	// todosUrl: " https://api.parse.com/1/classes/todos/"
 };

return helper;

});